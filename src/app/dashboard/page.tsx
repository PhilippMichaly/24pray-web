export default function DashboardPage() {
  return (
    <main className="p-6">
      <h1 className="font-display text-2xl font-bold">Dashboard</h1>
      <p className="mt-2 text-sm text-[var(--text-muted)]">
        Deine Projekte und Buchungen — wird in Phase 2 implementiert.
      </p>
    </main>
  );
}
